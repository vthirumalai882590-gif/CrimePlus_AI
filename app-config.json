{
	"command": "pip install -r requirements.txt && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
	"build_path": "apps/ml-service",
	"stack": "python3.12",
	"env_variables": {},
	"memory": 512,
	"scripts": {}
}
